apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: site-bff-service
spec:
  template:
    spec:
      containers:
        - image: site-bff
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 1000m
              memory: 512Mi